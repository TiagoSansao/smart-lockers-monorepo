spring:
  application:
    name: lockermanager

  datasource:
    url: jdbc:h2:file:./data/lockermanager-db
    driverClassName: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  spring:
    h2:
      console:
        enabled: true
        path: h2-console